{
  "name": "things",
  "version": "2.0.0",
  "private": true,
  "description": "Costa's things feed — links, ideas, notes, photos. Static site built from entries/*.json.",
  "scripts": {
    "build": "node src/build.js",
    "start": "node server.js",
    "validate": "node src/validate.js",
    "migrate": "node scripts/migrate-index.js"
  },
  "engines": {
    "node": ">=22"
  }
}
